# TODO - Fix Uncaught runtime errors

- [x] Inspect `ProductDetails.js` and update it to guard against undefined `product.images` and non-array `reviews`.
- [x] Correct `addReview()` to call the reviews API endpoint (not `cart/add.php`).

- [ ] Re-run the dev server / reload product pages to verify no runtime error.
- [ ] Validate product image rendering when backend returns no `images`.






